#
# Ted's global Git configuration
#
# Revision: 0   2014-11-09T23:56:09Z
#

[user]
    name = Ted Burghart
    email = ted@tedb.net

[credential]
    helper = osxkeychain

[core]
    excludesfile = ~/.gitignore_global
    whitespace = blank-at-eol,space-before-tab,tab-in-indent,-indent-with-non-tab,-blank-at-eof
    eol = lf

[commit]
    cleanup = whitespace
    status = true

[filter "media"]
    required = true
    clean = git media clean %f
    smudge = git media smudge %f

# [filter "updaterev"]
#     required = true
#     clean = ~/prj/tedb/src_tools/bin/update-rev.sh %f
#     smudge = cat

[difftool "sourcetree"]
    cmd = opendiff \"$LOCAL\" \"$REMOTE\"

[mergetool "sourcetree"]
    cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
    trustExitCode = true
